import domReady from '@wordpress/dom-ready';
import apiFetch from '@wordpress/api-fetch';

domReady( () => {
	// Admin entry point — add feature modules here.
	console.log( 'Stagify admin loaded' );
} );

export {};
